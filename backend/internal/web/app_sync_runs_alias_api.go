package web

// Compatibility note:
// The /api/v1/admin/sync-runs routes remain compatibility aliases at the routing layer,
// but they are now backed by dedicated sync run handlers rather than the legacy sync-jobs wrapper.
