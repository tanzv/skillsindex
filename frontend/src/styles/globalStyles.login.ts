import { globalLoginStylesComponents } from "./globalStyles.login.components";
import { globalLoginStylesFormHeader } from "./globalStyles.login.formHeader";
import { globalLoginStylesInfoCards } from "./globalStyles.login.infoCards";
import { globalLoginStylesResponsive } from "./globalStyles.login.responsive";
import { globalLoginStylesTheme } from "./globalStyles.login.theme";

export const globalLoginStyles = `
${globalLoginStylesTheme}
${globalLoginStylesInfoCards}
${globalLoginStylesFormHeader}
${globalLoginStylesComponents}
${globalLoginStylesResponsive}
`;
